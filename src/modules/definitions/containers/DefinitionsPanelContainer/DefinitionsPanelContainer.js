import React from 'react';

// TODO: build queries for definitions panel container from graphql endpoint

const DefinitionsPanelContainer = () => (
	<div />
);

export default DefinitionsPanelContainer;
